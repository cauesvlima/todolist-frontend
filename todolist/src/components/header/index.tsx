import styles from './styles.module.scss';

export const Header = () =>{
    return(
        <header className={styles.headerBackground}>
            <h1>
                Lista de tarefas
            </h1>
        </header>
    )
}